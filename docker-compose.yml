version: '3'
services: 
  app: 
    build: 
      context: .
      dockerfile: Dockerfile
    ports: 
      - "8081:8081"
    environment: 
      DB_HOST: "jdbc:postgresql://${IP}:5432/seller-panel"
      DB_USER: "${DB_USER}"
      DB_PASS: "${DB_PASS}"